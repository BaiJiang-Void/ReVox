# ReVox Core Enhancer Dependencies
opencv-python>=4.5.0
noisereduce>=2.0.0
soundfile>=0.10.0
numpy>=1.20.0
librosa>=0.9.0

# If user wants to run everything in one environment, 
# they should also install SadTalker's requirements:
# -r ../SadTalker/requirements.txt (Optional reference)